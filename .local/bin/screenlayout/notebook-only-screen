#!/bin/sh

# xrandr --output eDP-1 --primary --mode 1920x1080 --pos 0x0 --rotate normal --output HDMI-1 --off --output DP-1 --off --output DP-2 --off --output DP-3 --off --output DP-4 --off

# reload i3 + polybar
i3-msg restart

# conky restart
# killall conky && $HOME/.config/conky/conkyx-start.sh &

# background fix
nitrogen --restore
