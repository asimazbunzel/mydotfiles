#!/bin/sh

name="telegram"

tmux has-session -t "$name" 2>/dev/null

if [ $? != 0 ]
then
   st -e tmux new-session -d -s "$name" -n telegram
   st -e tmux send-keys -t "$name".0 "tg" ENTER
fi

exec st -e tmux attach -t "$name"
